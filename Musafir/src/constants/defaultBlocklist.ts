/**
 * App Constants
 * Note: Blocklist is now handled by AI Content Filter - no static list needed
 */

// Legacy blocklist (kept for backward compatibility, but no longer used for filtering)
export const DEFAULT_BLOCKLIST: string[] = [];

export const APP_NAME = 'Musafir';
export const NOTIFICATION_CHANNEL_ID = 'musafir_timer';
export const NOTIFICATION_CHANNEL_NAME = 'Timer Service';
export const VPN_NOTIFICATION_ID = 1001;
export const TIMER_NOTIFICATION_ID = 1002;
